# =============================================================================
# bootstrap.env - Bootstrap Configuration
# =============================================================================
# Controls init data size and benchmark duration.
# Use small values for testing e2e flow, large values for actual benchmarks.
# =============================================================================

# Mode: "test" (fast init) or "bench" (full data)
BOOTSTRAP_MODE="test"

# -----------------------------------------------------------------------------
# Data Initialization
# -----------------------------------------------------------------------------
# pgbench scale factor (1 scale = ~16MB, 100K rows)
#   test:  100  (~1.6GB, ~1min init)
#   bench: 1250 (~20GB, ~10min init)
PGBENCH_SCALE=100

# sysbench TPC-C warehouses (1 warehouse = ~100MB)
#   test:  20  (~2GB, ~2min init)
#   bench: 200 (~20GB, ~20min init)
SYSBENCH_WAREHOUSES=20

# -----------------------------------------------------------------------------
# Benchmark Duration
# -----------------------------------------------------------------------------
# pgbench duration in seconds
#   test:  30
#   bench: 300
PGBENCH_DURATION=30

# sysbench duration in seconds
#   test:  30
#   bench: 300
SYSBENCH_DURATION=30

# FIO duration in seconds
#   test:  10
#   bench: 60
FIO_DURATION=10
