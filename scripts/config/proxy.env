# =============================================================================
# PROXY CONFIG: c8g.xlarge
# =============================================================================
# Instance: c8g.xlarge (Compute Optimized, Graviton4)
# vCPU: 4 | RAM: 8 GB (approx)
# Role: Benchmark Client / Proxy
# =============================================================================

# === INSTANCE SPECS ===
INSTANCE_TYPE="c8g.xlarge"
VCPU=4
RAM_GB=8

# === BENCHMARK CLIENT CONFIG ===
PGBENCH_DURATION=60
PGBENCH_THREADS=4
PGBENCH_CLIENTS=100
