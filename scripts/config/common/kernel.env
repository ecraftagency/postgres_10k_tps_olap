# =============================================================================
# COMMON KERNEL CONFIG: Scheduler/NUMA Tuning (ALL nodes)
# =============================================================================
# Apply to ALL nodes: primary, sync-replica, async-replica, proxy
# Optimizes kernel scheduler for database workloads
# =============================================================================

# =============================================================================
# KERNEL / SCHEDULER
# =============================================================================
# Disable autogroup - prevents process grouping that hurts DB performance
KERNEL_SCHED_AUTOGROUP_ENABLED=0

# Disable NUMA balancing - PostgreSQL manages its own memory locality
KERNEL_NUMA_BALANCING=0

# Semaphore limits for PostgreSQL
KERNEL_SEM="250 32000 100 128"
