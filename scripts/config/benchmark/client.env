# =============================================================================
# BENCHMARK CLIENT CONFIG: pgbench/sysbench parameters
# =============================================================================
# Apply to: proxy node (benchmark client)
# Instance: c8g.xlarge (Compute Optimized, Graviton4)
# vCPU: 4 | RAM: 8 GB
# =============================================================================

# === INSTANCE SPECS ===
INSTANCE_TYPE="c8g.xlarge"
VCPU=4
RAM_GB=8

# === PGBENCH CONFIG ===
PGBENCH_SCALE=1250
PGBENCH_DURATION=60
PGBENCH_THREADS=4
PGBENCH_CLIENTS=100

# === SYSBENCH CONFIG ===
SYSBENCH_WAREHOUSES=200
SYSBENCH_DURATION=60
SYSBENCH_THREADS=4

# === FIO CONFIG ===
FIO_DURATION=60
